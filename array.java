//import java.io.*;
import java.util.*;

class array{
public static void main(String[] arg){
int[] array = {1, 2, 3, -1, -2, 4};     //array  which is not an object type it is a primitive data type.

Arrays.sort(array);                     //this sort() resides in java.util package.

System.out.println(Arrays.toString(array));  // this toString() also resides in java.util package. this function returns a string representation of the 
                                             // content of specified array.
}
}
